---
tags: [meta-king, auditor, security]
version: 1.0.0
created: 2026-07-16
replaces: 07-安全系统.md
---

# 07 Auditor 四维审查

> Auditor 横切所有认知体的输入和输出,四维审查。

## 审查矩阵

| 维度 | 触发时机 | 关注点 | 拦截动作 |
|------|---------|--------|----------|
| **安全** | 输入前 / 执行前 | 注入攻击、越权操作、破坏性指令 | 拒绝 + 说明原因 |
| **合规** | 规划后 / 产出前 | 策略对齐、范围越界 | 拒绝 + 建议修正 |
| **质量** | 产出后 | 完整性、正确性、一致性 | 评分 + 修改建议 |
| **恢复** | 失败后 | 异常分类、回滚路径 | 触发回滚 + 记录 |

## 执行规则

- 安全/合规维度 → **必须通过**才能继续
- 质量维度 → 低于 0.7 分需修正后重审
- 恢复维度 → 失败时**自动触发**

## 与 Gate 三步的关系

Auditor 的"安全"维度对应 [[01-MetaKing调度中枢]] 中的 **Gate C**:

```
Gate C — 安全边界
  → "这么做安全吗?" → 删除/覆盖/非只读 → 自动挂起 → 强制二次确认
```

Gate C 是硬约束 — **写入态操作无论用户是否主动要求,必须经过确认**。这不是"不信任用户",而是"不假设用户知道你将要做什么"。

## Auditor 在生产链中的位置

```
Planner → Thinker → Repair → Searcher(如需) → Builder → Auditor
                                                      │
                                            ┌─────────┴─────────┐
                                            ▼                   ▼
                                       安全/合规维度         质量维度
                                       必须通过              评分 ≥ 0.7
                                            │                   │
                                            └─────────┬─────────┘
                                                      ▼
                                                  Recorder
                                                  (记录决策)
```

## 失败回滚协议

| 失败类型 | 回滚动作 |
|---------|---------|
| 工具不可用 | 切换到同功能替代工具 |
| 认知体越界 | 代理到正确认知体 |
| Provider 无响应 | 切换到备用 Provider |
| 外部 API 断开 | 触发重试 + 降级策略 |

## 与老 KING 安全系统的对照

| 老 KING v3.0 (安全系统) | MetaKing v1.0 (Auditor) |
|------------------------|-------------------------|
| 四级权限(L0/L1/L2/L3) + 注入检测三层路径 | 四维审查(安全/合规/质量/恢复) |
| 高危命令拦截(黑名单) | Gate C 写入前对账(白名单思维) |
| sec_check_injection / sec_check_command 两个工具 | Auditor 横切拦截,不预注册工具 |

**关键差异**:老 KING 是"黑名单拦截"(列出危险命令),MetaKing 是"白名单+对账"(默认只读,写入必须显式确认)。后者在工程上更安全,因为攻击向量无穷无尽但写入操作有穷。

## 关联

- 上游: [[04-Builder任务生产链]] (执行后审查)
- 横切: [[01-MetaKing调度中枢]] Gate C
- 下游: [[08-Recorder与决策链]] (失败案例送入 inspiration_pool)